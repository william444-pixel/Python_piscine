[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "0.1.0"
description = "A-maze-ing generator for 1337 project"

[project.scripts]
mazegen = "mazegen.generator:main"

[tool.setuptools]
packages = ["mazegen"]